<?php

namespace {{ namespace }};

use Symfony\Component\Routing\Attribute\Route;
use OpenApi\Annotations as OA;
{% for use in uses %}
use {{ use }};
{% endfor %}

/**
 * @OA\Tag(name="{{ entity_name }}")
 */
#[Route(path: '/{{ entity_url_name }}', name: '{{ route_name_prefix }}')]
class {{ entity_name }}Controller extends {{ parent }}
{
    public const string entityClass = {{ entity_name }}::class;
    public const array serializationGroups = [{{ serialization_groups|raw }}];
    public const string entityCreateTypeClass = {{ entity_name }}Type::class;
    public const string entityUpdateTypeClass = {{ entity_name }}Type::class;
    public const array listSerializationGroups = [{{ serialization_groups|raw }}];
    public const array filterFields = [{{ nativeFieldsNames|raw }}];
    public const array filterSortFields = [{{ nativeFieldsNames|raw }}];

    use CrudControllerTrait;
}
