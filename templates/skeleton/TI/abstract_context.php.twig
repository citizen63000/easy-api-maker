<?php

namespace {{ namespace }};

use App\Tests\Functional\AbstractFunctionalTest;
use {{ entity_use }};

abstract class {{ abstractContextName }} extends AbstractFunctionalTest
{
    protected const ?string baseRouteName = '{{ route_name_prefix }}';
    protected const ?string entityClass = {{ entity_name }}::class;

    protected static array $additionalInitFiles = ['{{ additionalInitFile }}'];

    protected static function getRequiredFields(): array
    {
        return [{{ requiredFieldsForArray|raw }}];
    }
}
